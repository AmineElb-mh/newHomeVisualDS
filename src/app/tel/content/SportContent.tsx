import MakeTelSport from "../../../imports/make/TelSport/index";
import MakeContentOnly from "./MakeContentOnly";

export default function SportContent() {
  return <MakeContentOnly Page={MakeTelSport} />;
}
